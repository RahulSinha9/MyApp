jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    ...
    - name: Deploy to Staging
      if: success()
      run: |
        docker pull <your-dockerhub-username>/myapp-go:latest
        docker-compose -f docker-compose.yml up -d
